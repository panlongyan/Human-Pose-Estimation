# 相机内参标定流程

本文档整理单相机内参和畸变标定的一般流程。它适用于针孔、鱼眼、等距等常见相机模型，也适用于后续 VIO/SLAM 系统中把像素点转换为归一化相机坐标的场景。

## 1. 准备标定板

常见标定板是棋盘格。标定前需要明确两个参数：

```text
内角点数量：例如 12 x 8
格子尺寸：例如 80 mm
```

通用标定命令通常会包含标定板尺寸、图片目录和相机模型类型。例如：

```bash
camera_calibration_tool --cols 12 --rows 8 --square-size 80 --input ./calibration_images --model pinhole
```

参数含义：

```text
--cols 12              每行 12 个内角点
--rows 8               每列 8 个内角点
--square-size 80       格子边长 80，单位通常是 mm
--input ./images       标定图片目录
--model pinhole        使用针孔相机模型
```

注意：`w` 和 `h` 通常表示棋盘格的内角点数量，不是黑白方块数量。

## 2. 采集标定图片

需要拍摄多张标定板图片，让棋盘格覆盖图像的不同区域、不同尺度和不同姿态。

推荐覆盖：

```text
正对相机
左上角
右上角
左下角
右下角
靠近图像边缘
远距离
近距离
俯仰倾斜
左右偏转
平面内旋转
```

采集目标：

```text
让角点尽可能覆盖整张图像，尤其是图像边缘。
```

原因是镜头畸变通常在图像边缘最明显。如果标定板只出现在图像中心，畸变参数会估计不充分，后续 VIO/SLAM 的重投影误差会变大。

## 3. 检测棋盘格角点

标定程序会在每张图片中检测棋盘格内角点，得到图像坐标：

```text
(u1, v1), (u2, v2), (u3, v3), ...
```

因为标定板尺寸已知，程序也知道这些角点在标定板坐标系中的 3D 坐标：

```text
(0, 0, 0)
(80, 0, 0)
(160, 0, 0)
...
```

棋盘格是平面，所以所有角点的 `z = 0`。

## 4. 建立相机投影模型

每张图片中，同一个标定板点会经过以下几何链路投影到图像上：

```text
标定板 3D 点
  -> 通过该图片对应的外参变换到相机坐标系
  -> 通过相机内参投影到图像平面
  -> 通过畸变模型修正
  -> 得到预测像素点
```

简写为：

```text
P_board -> P_camera -> p_pixel
```

标定算法会比较：

```text
预测像素点 vs 实际检测到的棋盘格角点
```

两者的差值就是重投影误差。

## 5. 优化相机参数

标定时一般同时优化两类参数。

全局共享参数：

```text
相机内参：fx, fy, cx, cy
畸变参数：k1, k2, p1, p2 ...
```

每张图片独立参数：

```text
标定板相对相机的位姿：R, t
```

每张图片中标定板的姿态不同，所以每张图片都有自己的 `R, t`。但所有图片共享同一套相机内参和畸变参数。

优化目标：

```text
最小化所有图片、所有角点的重投影误差
```

重投影误差：

```text
检测到的角点像素位置 - 用当前参数预测出来的角点像素位置
```

## 6. 输出标定文件

标定完成后会输出相机模型文件，通常是 YAML 格式，例如：

```yaml
model_type: PINHOLE
camera_name: camera
image_width: 640
image_height: 480

projection_parameters:
  fx: ...
  fy: ...
  cx: ...
  cy: ...

distortion_parameters:
  k1: ...
  k2: ...
  p1: ...
  p2: ...
```

这些参数描述了相机如何把 3D 空间点投影成 2D 像素点，也描述了如何把像素点反投影成相机坐标系中的一条光线。

## 7. 在 VIO/SLAM 中使用标定结果

在 VIO/SLAM 系统中，通常会在配置文件中引用标定结果：

```yaml
camera_calibration: "camera_intrinsics.yaml"
```

前端检测和跟踪到像素点后，会调用相机模型做去畸变和归一化：

```text
像素点 (u, v)
  -> 去畸变
  -> 归一化相机坐标 (x, y, 1)
```

后端优化使用的是归一化相机坐标，而不是原始像素坐标。这样视觉重投影误差才能正确表达相机几何关系。

## 8. 判断标定质量

最常用指标是重投影误差：

```text
reprojection error
```

一般来说，普通相机希望重投影误差达到亚像素级，例如：

```text
< 0.5 pixel    较好
< 1.0 pixel    通常可用
```

实际阈值取决于：

```text
相机分辨率
镜头畸变程度
图片清晰度
标定板打印精度
角点检测质量
采集视角覆盖范围
```

如果重投影误差较大，通常需要检查：

```text
棋盘格内角点数量是否填错
格子尺寸是否填错
图片是否模糊
角点是否覆盖图像边缘
标定板是否平整
使用的相机模型是否合适
```

## 9. 一句话总结

相机标定就是：

```text
用很多张已知真实几何的棋盘格图片，反推相机内参和畸变参数，
让系统以后能把任意像素点正确转换成相机坐标系中的光线。
```

对于 VIO/SLAM 来说，标定质量会直接影响特征去畸变、重投影误差、轨迹精度和系统稳定性。
